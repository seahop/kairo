name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v0.1.0)'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  # First, create the release so build jobs can upload to it
  create-release:
    runs-on: ubuntu-latest
    outputs:
      release_id: ${{ steps.create_release.outputs.id }}
      release_upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Kairo v${{ steps.get_version.outputs.VERSION }}
          draft: false
          prerelease: ${{ contains(github.ref_name, 'beta') || contains(github.ref_name, 'alpha') || contains(github.ref_name, 'rc') }}
          generate_release_notes: true
          body: |
            ## Downloads

            ### Installers
            | Platform | Download |
            |----------|----------|
            | Linux (Debian/Ubuntu) | `.deb` |
            | Linux (Fedora/RHEL) | `.rpm` |
            | Linux (Portable) | `.AppImage` |
            | Windows | `.msi` or `.exe` (NSIS) |
            | macOS (Apple Silicon) | `.dmg` (arm64) |
            | macOS (Intel) | `.dmg` (x64) |

            ### Standalone Binaries
            | Platform | Binary |
            |----------|--------|
            | Linux | `kairo-linux` |
            | Windows | `kairo-windows.exe` |
            | macOS (Apple Silicon) | `kairo-macos-arm` |
            | macOS (Intel) | `kairo-macos-intel` |

            ### Installation Notes
            - **deb**: `sudo dpkg -i kairo_*.deb && sudo apt-get install -f`
            - **rpm**: `sudo dnf install kairo-*.rpm` or `sudo rpm -i kairo-*.rpm`
            - **AppImage**: Make executable with `chmod +x` and run directly
            - **Windows**: The NSIS `.exe` is an installer, `.msi` is for enterprise deployment
            - **macOS**: Open the `.dmg` and drag Kairo to Applications
            - **Standalone binaries**: Require system GTK/WebKit libraries to be installed
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build:
    needs: create-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-22.04
            target: linux
            bundles: deb,rpm,appimage
          - platform: windows-latest
            target: windows
            bundles: msi,nsis
          - platform: macos-15
            target: macos-intel
            bundles: dmg
            rust_target: x86_64-apple-darwin
          - platform: macos-14
            target: macos-arm
            bundles: dmg
            rust_target: aarch64-apple-darwin

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install Linux dependencies
        if: matrix.target == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libsoup-3.0-dev \
            libjavascriptcoregtk-4.1-dev \
            rpm

      # Ensure x86_64 target is available for macOS Intel cross-compilation
      - name: Add x86_64 target for macOS Intel
        if: matrix.target == 'macos-intel'
        run: rustup target add x86_64-apple-darwin

      - name: Install frontend dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build frontend
        run: pnpm build

      - name: Build Tauri app
        run: pnpm tauri build --bundles ${{ matrix.bundles }} ${{ matrix.rust_target && format('--target {0}', matrix.rust_target) || '' }}

      # Linux uploads
      - name: Upload Linux deb to release
        if: matrix.target == 'linux'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: src-tauri/target/release/bundle/deb/*.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Linux rpm to release
        if: matrix.target == 'linux'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: src-tauri/target/release/bundle/rpm/*.rpm
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Linux AppImage to release
        if: matrix.target == 'linux'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: src-tauri/target/release/bundle/appimage/*.AppImage
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Linux binary to release
        if: matrix.target == 'linux'
        shell: bash
        run: |
          cp src-tauri/target/release/kairo kairo-linux
          gh release upload ${{ github.ref_name }} kairo-linux --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Windows uploads
      - name: Upload Windows msi to release
        if: matrix.target == 'windows'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: src-tauri/target/release/bundle/msi/*.msi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows nsis to release
        if: matrix.target == 'windows'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: src-tauri/target/release/bundle/nsis/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows binary to release
        if: matrix.target == 'windows'
        shell: bash
        run: |
          cp src-tauri/target/release/kairo.exe kairo-windows.exe
          gh release upload ${{ github.ref_name }} kairo-windows.exe --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # macOS uploads
      - name: Upload macOS dmg to release
        if: startsWith(matrix.target, 'macos')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: src-tauri/target/${{ matrix.rust_target }}/release/bundle/dmg/*.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload macOS binary to release
        if: startsWith(matrix.target, 'macos')
        shell: bash
        run: |
          cp src-tauri/target/${{ matrix.rust_target }}/release/kairo kairo-${{ matrix.target }}
          gh release upload ${{ github.ref_name }} kairo-${{ matrix.target }} --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Also upload as GitHub Actions artifacts for debugging
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kairo-${{ matrix.target }}
          path: |
            src-tauri/target/release/bundle/**/*
            src-tauri/target/${{ matrix.rust_target }}/release/bundle/**/*
          if-no-files-found: warn
